import { TitleFormatDirective } from './title-format.directive';

describe('TitleFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleFormatDirective();
    expect(directive).toBeTruthy();
  });
});
